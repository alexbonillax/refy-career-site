import { createTheme } from "@mui/material";


const MaterialUiTheme = createTheme({
  typography: {
    fontFamily: [
      "Nexa",
      "Helvetica Neue",
      "sans-serif"
    ].join(",")
  }
});

export default MaterialUiTheme;